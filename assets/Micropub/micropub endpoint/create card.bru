meta {
  name: create card
  type: http
  seq: 1
}

post {
  url: {{base_url}}/micropub
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "h": "card",
    "name": "Giacomo Debidda",
    "job-title": "developer"
  }
}
